var cocossd = "";

webcam(300, 450);

function setup()
{
    createCanvas(450, 300, 0, 0, )
}

function start()
{
    modelLoad(cocossd);
    if(cocossd = true)
    {
        results("Estado:detectando objetos");
    }
}

function modelLoaded()
{
    if(status = true)
    {
    console.log("Modelo cargado");
    }
}

function draw()
{
    canvas = webcam;
}